name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Verify (Node ${{ matrix.node }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: ['20', '22']

    steps:
      - uses: actions/checkout@v6

      - name: Setup Node ${{ matrix.node }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build library packages
        run: npm run build

      - name: Typecheck (all tsconfigs)
        # Examples import from `@rekurt/ohlcv-core` which resolves to
        # `packages/core/dist/index.d.ts`. Typecheck must run AFTER the
        # build step or the example tsconfigs can't find the core type
        # declarations in a fresh `npm ci` environment.
        run: npm run typecheck

      - name: Run unit tests
        run: npm test -- --run

      - name: Build examples (smoke)
        run: npm run build:examples

      - name: Report bundle size
        if: matrix.node == '22'
        run: npm run size
        continue-on-error: true
